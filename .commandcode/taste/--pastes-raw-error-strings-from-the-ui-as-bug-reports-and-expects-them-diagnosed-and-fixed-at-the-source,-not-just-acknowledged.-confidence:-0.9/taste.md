# - Pastes raw error strings from the UI as bug reports and expects them diagnosed and fixed at the source, not just acknowledged. Confidence: 0.9
- Pastes raw error strings from the UI as bug reports and expects them diagnosed and fixed at the source, not just acknowledged. Confidence: 0.9
- When a plan is rejected because the user made uncommitted changes, re-verify every load-bearing claim against the new HEAD (git status / git diff / git log) before rewriting the plan. Confidence: 0.95
- Uses mid-turn one-line redirects like "lets go" / "continue" / "OK" to authorize proceeding without further questions — resume directly without recap. Confidence: 0.85
- Picks scope via AskUserQuestion options rather than free-form answers; honors the chosen scope precisely without adding unrequested work. Confidence: 0.7
- Iterates on the same surface (home / brief) across many turns; expects each turn's changes to build on the last without redoing prior work. Confidence: 0.8
- Product/design direction for the personal AI: "no OS, no bullshit" — text-first single-agent experience with minimal cards, cards used only for organization (not a feature-heavy app-store/OS feel). Confidence: 0.9
- Values agentic autonomy and proactivity ("it does it for me, hands off, proactively follows up") as the primary "better than Instinct" lever — more important than card/UI trimming; wants the agent to complete work rather than just draft, and to recur/re-arm rather than fire once. Confidence: 0.85
- Approval model: agent does everything, but any real-world side effect requires a one-tap release ("do it, one tap to approve") — keep the guardrail against unapproved sends/actions. Confidence: 0.8
- Dislikes brittle regex parsing of model output — prefers tolerant, fail-open parsing (balanced-JSON extractor honoring quotes/escapes, narrated/fenced replies) so quirks in model output don't silently drop or blank an important section. Confidence: 0.85
- Design principle for the brief/data pipeline: "filter broad, judge narrow, fail open" — let the model (not coarse pre-filters like Gmail tab categories) decide what's noise, raise pull caps/windows so nothing is truncated unseen, and on judge failure fall back to buckets rather than wiping the section. Applies across all data surfaces in the brief (mail, calendar, etc.) — once fixed for one surface, the user asks the same question for the next ("calendar too is good?"). Confidence: 0.85
- Even under the "no OS, text-first" direction, the persistent 4-tile bottom dock on the mini-app home screens (Brief/People/Later + persona-specific quick-travel buttons) is a valued navigation surface that must be kept — do not remove the dock/navigation in cleanup or minimalism passes; if removed in one change, immediately restore it on all home variants. Confidence: 0.8
- Expects periodically-rebuilt data (the briefing) to reflect freshly landed input on reopen; serving cached/stale "old data" that hides new mail is reported as a bug to diagnose and fix at the source freshness/validated-cache layers, not accepted as caching behavior (tighten server TTL + stale-while-revalidate windows rather than wave off). Holds across every layer: browser SWR, in-memory stale cache, persisted DB row, free-tier rationing path — the user says "still showing old data" and expects the trace to keep going until the real culprit is found, including cross-day rollover bugs and rationing stale-fallbacks. Confidence: 0.8
- Writes in lowercase, informal, run-on style with dropped apostrophes ("its not updating though", "bring it back", "lets go") — matches that register in conversational replies rather than formalizing it. Confidence: 0.7
